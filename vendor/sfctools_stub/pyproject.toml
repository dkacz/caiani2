[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sfctools"
version = "0.0.0"
description = "Local stub for sfctools FlowMatrix used for CI smoke."
requires-python = ">=3.8"
authors = [{name="Local Stub"}]
